id: geyserextra
name: GeyserExtra
main: com.geyserextra.extension.GeyserExtraExtension
# api 2.9.3+ is required because this extension uses v2 CustomItemDefinition
# (org.geysermc.geyser.api.item.custom.v2.*). Declaring 2.10.0 keeps it aligned
# with the compile-time dependency in extension/build.gradle.kts; runtime Geyser
# must be at least 2.9.3 for the v2 types to resolve.
api: 2.10.0
version: 1.0.0
authors: [GeyserExtra]
